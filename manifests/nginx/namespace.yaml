apiVersion: v1
kind: Namespace
metadata:
  name: nginx
  labels:
    name: nginx
    # pod-security.kubernetes.io/enforce: restricted
---
# default deny all
# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: default-deny
#   namespace: nginx
# spec:
#   podSelector: {}
#   policyTypes:
#     - Ingress
#     - Egress